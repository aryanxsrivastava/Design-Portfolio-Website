# === DEPENDENCIES ===
node_modules/
.pnp/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# === NEXT.JS BUILD OUTPUT ===
.next/
out/         # ← export output folder (you don't want to commit this)
build/       # sometimes used in CI/CD or custom builds

# === TEST COVERAGE ===
coverage/

# === SYSTEM FILES ===
.DS_Store
*.pem

# === LOGS & DEBUG FILES ===
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# === DEPLOYMENT ===
.vercel      # if you've ever used Vercel locally
.netlify     # (optional) if Netlify CLI was used locally

# === TYPESCRIPT ===
*.tsbuildinfo
next-env.d.ts

# === ENVIRONMENT VARIABLES ===
.env*
!.env.example   # <-- include this if you have a template file

# === GENKIT (optional Google Genkit) ===
.genkit/

# === FIREBASE DEBUG (if used) ===
firebase-debug.log
firestore-debug.log
